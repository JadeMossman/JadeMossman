#Activity 2 - Python Loops

#Write a Python program to create the multiplication table (from 1 to 10) of a number.

#Multiplication table (from 1 to 10) in Python


num = int(input("Display multiplication value of? :"))
 
for i in range(1, 11):
   print(num, 'x', i, '=', num*i)
